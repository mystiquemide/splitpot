# Optional — public EVM JSON-RPC for WDK wallet modules
# Defaults to a public Sepolia endpoint if unset
NEXT_PUBLIC_EVM_RPC_URL=https://sepolia.drpc.org

# Optional — canonical site URL for share links in production
NEXT_PUBLIC_APP_URL=http://localhost:3000
